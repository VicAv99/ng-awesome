import template from './posts.html';
import controller from './posts.controller';
import './posts.scss';

const bindings = {

}

let postsComponent = {
  bindings,
  template,
  controller
};

export default postsComponent;
